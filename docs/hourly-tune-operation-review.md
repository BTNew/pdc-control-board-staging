# Hourly Tune operation review

Craig requests a separate Updated operation lines section in New Vehicles for additions and modifications to vehicles already on the board. Preserve canonical identity, source evidence, bookings, location and completion history. Review by company/division + R/O + Line, with no age cutoff or omission-based deletion. Deduplicate overlapping exports and reject stale approvals.

Craig explicitly approved STAGING installation on 12 September. Applied database SQL is retained in supabase/proposals/tune_operation_change_review.sql and tune_operation_change_review_followups.sql. The existing hourly automation remains paused pending the previously required monitor handover; deployment does not activate it.

The UI shows accepted source, current workshop state and proposed source. Source hours remain immutable; approval changes only the selected effective line. Completed lines/stations and QC/RFT vehicles are held for rework review. Full approved source descriptions are projected without the old short display field truncation. Existing booking times remain unchanged and staff must review affected durations. Numeric parts observations advance independently while changed work awaits approval.

Local frontend suite: 382 passed on current STAGING main plus this change. Database checks passed: source comparisons, added/modified detection, scope/stock guards, anonymous denial, pending capture, repeat deduplication, supersession/reversion, stale snapshot changes, accepted baseline, approved description projection, pending parts observation time, and preservation of source operations, bookings and vehicle state. All test fixtures rolled back. New helpers are inaccessible to anonymous/authenticated clients; list/approval require the genuine approved staff role. The table denies direct client access. Security advisor notices for intentional authenticated functions and the deny-all table were reviewed.

Validation limit: no genuine signed-in browser was available. A new-file authenticated preview/apply and successful staff approval were not executed end to end; no real vehicle was approved or booked to test this feature. Approval performs atomic readback checks and rolls back on mismatch. Unsupported source identity/department conflicts remain held. Retain source evidence and inspect the first genuine changed-file receipt.

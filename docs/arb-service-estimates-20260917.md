# ARB estimate projection and import review — 17 September 2026

Missing Tune hours could remain zero even after catalogue research: the preparation step accepted a generic Unable, and unchanged-source imports saved revised estimates in history without updating the effective board display.

The service review projection now uses the latest successful, scoped, exact-operation catalogue evidence. It preserves immutable source operations and staff adjustments. Approved source changes are excluded, and report snapshot order protects against older evidence. The existing pending-review, booking, completion and approval guards remain in effect. AI provenance reaches the existing orange estimate display.

The shared preparation helper supplies verified MY26 HiLux Safari allowances using model/production evidence:2h V-Spec,2.5h ARMAX, or2.25h when unspecified (within15% of both). It does not infer model generation from email dates. Unsupported items require an interpreted Unable record with candidate/context evidence; merely running a keyword search is insufficient. All Department138 work remains Bus4x4. The original September PDFs/indexes are private runtime references and are not published here.

Validation: ten Python regression tests passed against the installed September index; database assertions covered the two snorkels, the owner's MY26 commercial-bar standard, positive Tune hours, staff zero, mismatched description, Sublet, owner standards, private access and Department138. Live readback confirmed the corrected catalogue values and labels. No original operation hours, saved staff estimates, bookings or inspection results were rewritten by this projection change.

The PO-upload February exact-part catalogue is a separate workflow. This release corrects the service importer; it does not claim that the PO catalogue was replaced.

For future preparation, enrich each row with `vehicle_context` from an exact current Navision match before `finalize_rows`, and retain every reviewed catalogue calculation in `arb_review`. New Unable records require `unable_reason_code`, `reviewed_candidates` and `vehicle_context.source`. Do not edit historical request bodies or hashes.


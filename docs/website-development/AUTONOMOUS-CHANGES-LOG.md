# Autonomous staging changes

## 2026-08-31 — isolated PDC Email AI transaction successor

- Dashboard association: `20260831_095314_64feeb`
- Isolated branch/worktree: `feature/pdc-email-ai-transaction-successor` at `C:/Users/nwmgr/HermesWorkspaces/development/pdc-email-ai-transaction-successor`.
- Added a four-layer STAGING-only successor: evidence-only RFC822 intake, strict typed planner, single canonical command/readback executor, synthetic acceptance, read-only poller fallback, version manifest, and runbook.
- Added append-only migration `supabase/staging_only/20260831300000_pdc_email_ai_transaction_successor.sql` plus forward repair `supabase/staging_only/20260831320000_pdc_email_ai_transaction_successor_contract_repair.sql` with forced-RLS immutable receipts, dedicated authenticated identity, fixed canonical dispatch, full source-graph binding, typed per-action dispositions, expected/actual response parity and authoritative snapshot readback.
- Current Email Monitor repair worktree/runtime/task/migrations were not changed. Production and outbound email were not contacted.
- Local evidence: 34 successor Python tests, full `npm run test` 219 passed/0 failed/1 skipped, `npm run check` passed, Python syntax passed, SQL parsed as 32 and 11 statements.
- Live STAGING migration/application and natural-cycle proof remain separate gates; the migration is guarded to the observed live 863 predecessor and must use the approved protected staging connector.
- Live apply/readback: `scripts/apply_pdc_email_ai_successor_staging.py` returned `ok=true` for ledger `20260831300000 / pdc_email_ai_transaction_successor`; read-only verifier returned `ok=true` with command/health RPCs present, authenticated execute true, service-role execute false, all successor tables forced RLS, zero runtime/transaction/action receipts, Production sentinel absent, and mailbox/task/outbound/UID514 untouched.
- Forward repair/readback: `scripts/apply_pdc_email_ai_successor_contract_repair_staging.py` returned `ok=true` for ledger `20260831320000 / pdc_email_ai_transaction_successor_contract_repair`; live function readback proved expected/actual action fields, source graph binding and distinct-vehicle version preflight.
- Added the staging-only chronological successor Intake surface: one parent row per email, separate vehicle results, received/sender/subject/Stock/UID/attachment summary, before→requested→result, verification, typed plan/version/action/RPC/receipt/retry/quarantine/readback details, loading/error/empty states, in-place Refresh and revision Realtime invalidation. The legacy `.68` panel remains retained but hidden as fallback; no legacy service request is made.
- Added append-only migrations `supabase/staging_only/20260831330000_pdc_email_ai_successor_inbox_read_projection.sql` and `supabase/staging_only/20260831340000_pdc_email_ai_successor_command_read_hardening.sql`; the live UI now uses v2 composite-cursor read RPC `get_pdc_email_ai_transaction_successor_inbox_v2`.
- Live STAGING read projection/hardening readback: ledger `20260831340000 / pdc_email_ai_successor_command_read_hardening`, inbox v2/RPC/revision/typed-plan/Realtime present, authenticated execute true, service-role execute false, revision RLS/FORCE RLS true, command identity binding and `confirmed=true` guard true, all receipt counts zero, Production sentinel absent, and mailbox/outbound/business mutation false.
- Pages UI release: clean staging-base release commit `fc173d87` was published to `main`; staging subsequently advanced independently while retaining the successor assets. Final cache-busted asset version `2026.08.31.08-successor-inbox-cursor-fix` is served with successful Pages/Staging integrity workflows. Authenticated browser proof: successor panel rendered 100 parent emails, v2 read RPC returned no raw body fields, Realtime revision channel joined, no mutation controls or production requests, zero horizontal overflow at 390px/1425px, and synthetic deployed renderer proof covered 2 parent emails/2 vehicles/2 actions/quarantine/details.
- Independent frozen-commit reviews were completed read-only; the first UI/command review was NO-GO on the pre-repair `da6c2dde` snapshot. The 3400 repair closed the reported identity-binding, `confirmed=true`, composite cursor, universal waiting status, legacy evidence-ref redaction, metadata/rules and Realtime-state gaps.
- Owner commissioning completed in STAGING through protected profile `.env` and the existing Auth admin helper, with no secret values printed or persisted in runtime. The dedicated Auth actor is bound to `pdc-email-ai-successor-069` / `pdc-emails`, versioned transport/model/prompt/taxonomy/rules/action contract, a one-shot provisioning receipt and DPAPI/ACL runtime store. Idempotent readback proved approved query/health/readback RPCs, hostile action, wrong actor/gateway, direct table and arbitrary SQL rejection; service-key-persisted and runtime-service-role are false.
- Natural pdc-emails proof remains the only open outcome gate: the owning mailbox transport must run the authorized `.69` evidence-only pickup and return sanitized source/plan/action/readback evidence before any claim of natural processing, replay/isolation or soak success.

## 2026-09-01 — typed least-authority action surface

- Added staging-only migration `supabase/staging_only/20260901020000_pdc_email_ai_typed_action_surface.sql` plus append-only strict-boundary repairs `20260901030000_pdc_email_ai_typed_action_boundary_repair_20260901.sql` and `20260901040000_pdc_email_ai_typed_action_validator_binding_20260901.sql`, guarded to the current predecessor chain and Production-sentinel absence.
- Added fixed typed successors for activation, operation add/update, Parts ETA/complete, booking set/move/cancel, required work, work complete, note, controlled location and supported RFT lifecycle. Canonical signatures are server-owned and were read-only verified against STAGING; no generic SQL, caller RPC/table names, service role or Administrator runtime path is accepted.
- Added forced-RLS action-rule metadata, immutable taxonomy-bearing action receipts, action-level `audit_events`, replay keys, field-level authoritative readback parity, strict server-side payload validation and typed taxonomy dispositions (`classified`, `review`, `unsupported`, `conflict`). Mixed FMG/GVM/GCM/Tare/decal descriptions remain review-only and cannot become Hoist/Sublet; unresolved 12V accessory sockets, safety triangles and weather shields remain typed REVIEW with no invented group.
- Updated the successor manifest and live read-only verifiers to the current 0400 STAGING head and v2 action contract. Migrations were applied through the serialized protected STAGING connector, but the action RPC was not invoked; no mailbox, outbound email, task enablement or Production access occurred.
- Verification: 62 focused Python successor/v2/action-surface tests passed; npm test/check each passed with 222 passed and 1 skipped; 0200/0300/0400 migrations parsed as 37/22/15 SQL statements; exact STAGING canonical signature probe passed for all 12 canonical functions. Post-apply verification passed with authenticated strict execute true, legacy direct typed execute false, service-role/public/anon execute false, forced RLS, immutable receipt/rules triggers, strict validator binding, operation-update path and zero receipts/business mutation. The v2 shadow runtime suite remains zero-write and separate from live action-RPC invocation.

- Append-only v2 contract-binding repair `supabase/staging_only/20260901050000_pdc_email_ai_typed_action_v2_contract_binding_20260901.sql` was applied to STAGING after the 0400 head. It binds the actual flattened v2 planner envelope (including source thread/message/digests and expected state) to the strict authenticated RPC, validates exact instruction/payload keys before source lookup/canonical dispatch, and preserves single-action operation-update concurrency/readback receipts.
- Live 0500 apply/readback returned `ok=true` at `cdsmnqxtyyoeoznmbidd` with authenticated strict execute true, legacy direct typed execute false, public/anon/service-role execute false, v2 validator present, all successor tables FORCE RLS, canonical signatures present, zero action/transaction receipts, no business mutation, mailbox/outbound/Production untouched. Migration SHA-256: `e861667965cfc5762d8a61082a1194e5be65c6a6cf0f26d34986031a45d79db9`.
- Final verification: v2/successor/action focused tests passed; `npm run test` and `npm run check` each passed 222/0/1; Python compilation passed; SQL parsed 0200/0300/0400/0500 as 37/22/15/16 statements. Anonymous REST remains correctly denied for the authenticated-only strict RPC; no action RPC was invoked.

## 2026-08-31 — repaired `.71` control-hash retry path

- Dashboard association: `20260828_191153_4fb787`; STAGING only; Production, outbound email, mailbox and task enablement were not contacted.
- Authoritative readback: `CURRENT=2026.08.69`; `PDC-PMB-Email-Monitor-Staging` remains `Disabled` / `LOCAL SERVICE` / `ServiceAccount` / `Limited` / `PT5M` / `LastTaskResult=1`; protected `.69` release ACL readback remains non-elevated `Access is denied`.
- Preserved failed `.71` evidence: `C:/Users/nwmgr/Desktop/PDCMonitor-Install-20260871/install-receipt.json` with `ok=false`, control-hash/access-denied detail, task disabled, and no mailbox/outbound/Production action.
- Root causes repaired in source/build controls: incompatible JSON-vs-tree control hash generation; non-canonical Windows-translated manifest line endings; missing staging `root-control` directory creation; and retry wrapper automation enablement before VerifyOnly/OneCycle gates.
- Rebuilt immutable retry bundle: `C:/Users/nwmgr/Desktop/PDCMonitor-Install-20260871-repaired/pdc-monitor-staging-m502-2026.08.71`, 3,359 files, manifest SHA-256 `13f3affa82e334195b93126c09764c29b50da55e2339642063c2e94d22811c1f`, control-tree SHA-256 `3af444ce65b52935a13e57df17b7dfb3dd329719aff59775a8bc266d740b6749`, inventory and control-tree matches true, canonical LF manifest true.
- Prepared disabled-only one-click path: `C:/Users/nwmgr/Desktop/PDCMonitor-Install-20260871-repaired/PDCMonitor-Install-20260871-repaired.ps1`; no UAC was launched. Receipt: `C:/Users/nwmgr/Desktop/PDCMonitor-Install-20260871-repaired/corrected-071-control-hash-preparation-receipt.json`.
+- Final wrapper hardening pins the corrected manifest and bundled installer hashes before execution; automation enablement is not passed by the one-click path.
- Verification: `.71` contract suite `7/7` passed; Python compilation passed; absolute Windows PowerShell parsing passed; relevant bundle hash/inventory/control checks passed. The bundled legacy `verify_release.py` remains a separate incompatible 503-spec verifier and was not bypassed or changed.

## 2026-09-01 — typed action boundary hardening follow-up

- Added staging-only append-only migrations `supabase/staging_only/20260901060000_pdc_email_ai_typed_action_boundary_hardening_20260901.sql` and `supabase/staging_only/20260901070000_pdc_email_ai_typed_action_execution_readback_20260901.sql`. The 0600 boundary now binds exact source/identity/provenance/version values, recomputes taxonomy before operation updates, rejects manual/locked overlays, and preserves review-only operation evidence with unknown hours. The 0700 follow-up sends complete tri-state work-state maps, converts canonical exceptions to queued-failure receipts, and verifies exact affected booking fields and timeline/work projections.
- Updated `backend/pdc_email_ai_successor_contract.py` and `backend/pdc_email_ai_v2_actions.py` so Python validation matches the server taxonomy and strict provenance/identity contract; added `tests/test_pdc_email_ai_typed_action_boundary_0600.py` regressions.
- Applied both migrations through the protected STAGING connector at `cdsmnqxtyyoeoznmbidd`; live head is `20260901070000 / pdc_email_ai_typed_action_execution_readback_20260901`. Read-only verifiers passed with authenticated-only strict execute, public/anon/service-role execute denied, all successor tables FORCE RLS, canonical signatures present, zero action/transaction receipts, no business mutation, mailbox/outbound email or Production contact.
- Verification: 39 focused Python tests passed; `npm run test` and `npm run check` each passed 222/0/1; Python compilation passed; SQL parsed as 37/22/15/16/29/15 statements for 0200–0700; live immutable preflight rejected fabricated mixed-signage classification and provenance while accepting valid review/unknown-hours evidence.

## 2026-09-01 — typed action identity-contract follow-up

- Added staging-only append-only migration `supabase/staging_only/20260901080000_pdc_email_ai_typed_action_identity_contract_20260901.sql` and its protected apply/readback controller `scripts/apply_pdc_email_ai_typed_action_identity_contract_staging.py`.
- The 0800 boundary makes identity-conflict taxonomy consistent across planner/Python/PostgreSQL, validates exact attachment/top-level plan schema values, binds every supplied Stock/VIN/Navision source identity to the authoritative vehicle UUID, and permits only explicit unresolved review-note evidence without dispatch. The existing append-only action receipt surface now supports the no-vehicle review disposition with a restrictive scope check.
- Updated `backend/pdc_email_ai_v2_planner.py`, `backend/pdc_email_ai_v2_actions.py`, `backend/pdc_email_ai_v2_runtime.py`, `backend/pdc_email_ai_successor_contract.py`, `tests/test_pdc_email_ai_typed_action_boundary_0600.py`, and live verifiers. Planner UUID identity fields are normalized to the server contract; runtime validation receives the authoritative read-only context.
- Protected STAGING apply/readback returned `ok=true` at `cdsmnqxtyyoeoznmbidd`, migration SHA-256 `4d434313ea0dbb3fe392c558ca1231e0d9b13e27ee42dd03623e90790e3f13cf`. Read-only probes accepted a valid plan, rejected forged Stock and malformed attachment digests, accepted consistent conflict and unresolved-review evidence, confirmed nullable review receipt scope, and confirmed authenticated-only strict execute with public/anon/service-role denied.
- No action RPC, canonical business mutation, mailbox, outbound email or Production path was invoked; successor/action/transaction receipt counts remain zero.
